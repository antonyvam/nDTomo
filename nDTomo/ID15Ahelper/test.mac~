
# Block 0
def id15_user_mac '{
	# Block 1
	fileName = "test"
	cmd = sprintf("xrd_point_norot %s 500", fileName)
	eval(cmd)

	# Block 2
	fileName = "prefix"
	cmd = sprintf("xrd_point %s 250", fileName)
	p cmd
	eval(cmd)

}'
